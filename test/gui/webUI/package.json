{
    "name": "oidc-login",
    "version": "0.0.1",
    "scripts": {
        "oidc-login": "playwright test --grep @oidc",
        "oauth-login": "playwright test --grep @oauth"
    },
    "devDependencies": {
        "@playwright/test": "^1.27.1",
        "playwright": "^1.27.1"
    }
}
